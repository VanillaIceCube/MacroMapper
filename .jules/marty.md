## 2026-09-11 - MealItem Component Snapshot and Adjustment Draft Cleanup **Learning:** Stored `MealItem.component_snapshot` records may omit `"nutrients"` in legacy entries, requiring fallback check before returning cached snapshots. Temporary `MealProposal` records created during adjustment endpoint calls must be cleaned up across all exception types to prevent database record leakage. **Action:** Check `"nutrients" in component` before relying on stored snapshots, and wrap proposal generation in try...except Exception cleanup blocks.
