## 2026-09-17 - Meal adjustment provider patching **Learning:** Tests in `estimates` and `meals` patch `get_estimation_provider` on `views.py` modules. Services wrapping view actions must accept or invoke `get_estimation_provider` dynamically so mocked providers in tests continue to be routed properly. **Action:** Always pass or resolve provider getters dynamically in service helpers that serve DRF view endpoints.
