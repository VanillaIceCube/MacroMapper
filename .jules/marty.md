## 2026-08-31 - Proposal Draft Cleanup & Portion Options Guarding **Learning:** Temporary draft proposals created during AI adjustment workflows must be cleaned up across all exception paths (`EstimationProviderError`, `ValidationError`, and unexpected `Exception`) to prevent orphaned draft `MealProposal` records in the database. On the frontend, `portionOptions` and `selectedPortion` can be called with empty/null item structures or missing `portion_options` fields during initial component rendering or rapid edits. **Action:** Future runs touching proposal adjustment APIs or portion options should maintain `except Exception:` draft cleanup in backend views and defensive optional chaining/null guards in `mealItemPortions.js`.
